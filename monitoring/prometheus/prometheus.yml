# Configuration de scraping Prometheus.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml   # règles d'alerte montées depuis monitoring/alerts

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]

scrape_configs:
  - job_name: execution
    metrics_path: /metrics
    static_configs:
      - targets: ["execution:8000"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
