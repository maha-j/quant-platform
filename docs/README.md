# docs/

Documentation vivante du système.

- `architecture/` — schémas, décisions (ADR), frontières entre composants.
- `strategies/` — description, hypothèses et métriques de chaque stratégie.
- `runbooks/` — procédures d'exploitation (incidents, déploiement, rollback).
